{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "Set-Location 'C:\\Users\\Bruno\\OneDrive\\Educação\\@ claude_code'; git add -A; git diff --cached --quiet; if ($LASTEXITCODE -ne 0) { git commit -m \"auto-update $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')\"; git push } 2>$null",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
